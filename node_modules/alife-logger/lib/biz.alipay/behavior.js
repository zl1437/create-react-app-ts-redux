var util = require("../util");

module.exports = function(i, t) {
    var o = null, r = require("../common/miniBehavior")(i) || {};
    util.ext(i.prototype, {
        initBehavior: function() {
            if (!this.hasInitBehavior && !o && void 0 !== t && t) {
                try {
                    "function" == typeof r.consoleBehavior && r.consoleBehavior.call(this);
                } catch (i) {
                    util.warn("[arms] error in initBehavior", i);
                }
                return o = this, this.hasInitBehavior = !0, this;
            }
        }
    });
};