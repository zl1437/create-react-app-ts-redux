var util = require("../util"), FILTER_RULE = [ /log\.mmstat/, /arms-retcode[\w-]*\.aliyuncs/ ], HOLDING_INITIATOR = /link|script|img|css/;

module.exports = function() {
    var e = (util.win || {}).performance;
    if (!e || "function" != typeof e.getEntries) return null;
    e.getEntriesByType("resource");
};